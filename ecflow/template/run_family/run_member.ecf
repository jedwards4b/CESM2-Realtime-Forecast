%include <multinode.h>
%include <head.h>
source /etc/profile.d/z00_modules.sh
module use $HOME/%CESM_WORKFLOW%/CESM2-Realtime-Forecast/modulefiles
module load %CESM_WORKFLOW%
member=$(echo %TASK% | cut -c 12-)
echo %FCSTDATE% $member
cd %FCST_WORK%/%CESM_WORKFLOW%_%FCSTDATE%.$member
./case.submit --job case.run

%include <tail.h>