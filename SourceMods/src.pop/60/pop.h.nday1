	float z_t(z_t) ;
	float z_t_150m(z_t_150m) ;
	float z_w(z_w) ;
	float z_w_top(z_w_top) ;
	float z_w_bot(z_w_bot) ;
	float dz(z_t) ;
	float dzw(z_w) ;
	float SST(time, nlat, nlon) ;
	float SST2(time, nlat, nlon) ;
	float SSS(time, nlat, nlon) ;
	float HMXL_DR_2(time, nlat, nlon) ;
	float XMXL_2(time, nlat, nlon) ;
